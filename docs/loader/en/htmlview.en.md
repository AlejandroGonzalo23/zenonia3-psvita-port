# `loader/htmlview.h` — Design Architecture & Notes

Explanatory and architectural design notes extracted from source code and replaced with concise technical Doxygen blocks. This document preserves the reasoning ('why') separated from technical API documentation.

## `struct` (line ~6)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---

## `htmlview.h` (line ~13) (line ~13)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---

## `htmlview_draw` (line ~19)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---

## `htmlview_draw` (line ~25)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---

## `htmlview_scroll` (line ~32)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---

## `htmlview_native_size` (line ~38)

**Source File:** `loader/htmlview.h`

> Load app0:html/<name>.html (packaged in the VPK, see CMakeLists.txt).
> panel_w_px is the REAL width in screen pixels of the panel where it will be
> draw (to wrap the lines) -- calculate it with sw/sh
> androidui_draw(), not with the 400x240 logical space. Returns NULL if the
> file does not exist or the font (gfa_font_init) is not available.

---